---
// Thank You Page
---

<html>
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Golden Crow VS - Thank You</title>
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <link rel="manifest" href="/site.webmanifest">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
</head>
<body>
  <nav class="main-nav">
    <div class="nav-container">
      <div class="logo">
        <a href="/">
          <img src="/logo1024.png" alt="Golden Crow VS Logo" />
        </a>
      </div>
      <button class="mobile-menu-toggle" aria-label="Toggle navigation">
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
      </button>
      <div class="nav-links">
        <a href="/" class="nav-link">Home</a>
        <a href="/about" class="nav-link">About Us</a>
        <a href="/pocket-genes" class="nav-link">Pocket Genes</a>
        <a href="/contact" class="nav-link">Contact Us</a>
      </div>
    </div>
  </nav>

  <main class="thanks-page">
    <!-- Success Hero Section -->
    <section class="success-hero">
      <div class="hero-bg-effects">
        <div class="floating-orb orb-1"></div>
        <div class="floating-orb orb-2"></div>
        <div class="floating-orb orb-3"></div>
      </div>
      <div class="container">
        <div class="success-content">
          <div class="success-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
              <polyline points="22 4 12 14.01 9 11.01"/>
            </svg>
          </div>
          <h1>Your Call Was Scheduled Successfully!</h1>
          <p class="success-message">We'll contact you via WhatsApp to confirm the details of your consultation. Get ready to start transforming your idea into reality.</p>
        </div>
      </div>
    </section>

    <!-- Who Is This For Section -->
    <section class="eligibility-section">
      <div class="container">
        <h2>This Consultation Is Perfect For</h2>
        <div class="eligibility-grid">
          <div class="eligibility-card">
            <div class="eligibility-icon">🏢</div>
            <h3>Companies</h3>
            <p>Looking to develop custom mobile software to improve operations or reach new customers</p>
          </div>
          <div class="eligibility-card">
            <div class="eligibility-icon">📈</div>
            <h3>SMEs</h3>
            <p>Pursuing digital transformation and wanting to leverage mobile technology for growth</p>
          </div>
          <div class="eligibility-card">
            <div class="eligibility-icon">🚀</div>
            <h3>Startups</h3>
            <p>Validating business ideas and needing an MVP to test the market quickly</p>
          </div>
          <div class="eligibility-card">
            <div class="eligibility-icon">💡</div>
            <h3>Entrepreneurs</h3>
            <p>With innovative app ideas ready to turn concepts into functional products</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Success Stories Section -->
    <section class="stories-section">
      <div class="container">
        <h2>Success Stories</h2>
        <p class="section-subtitle">Discover how we've helped companies like yours achieve their goals through mobile innovation</p>

        <div class="stories-grid">
          <div class="story-card">
            <div class="story-content">
              <div class="quote-icon">"</div>
              <p>"Golden Crow VS transformed our fitness coaching business. The StrongerU app has helped us scale to thousands of users while maintaining personalized experiences."</p>
              <div class="story-author">
                <div class="author-info">
                  <strong>Fitness Brand Executive</strong>
                  <span>StrongerU</span>
                </div>
              </div>
            </div>
          </div>

          <div class="story-card">
            <div class="story-content">
              <div class="quote-icon">"</div>
              <p>"The team understood our complex genomics requirements perfectly. PocketGenes is now the leading consumer genetics app in our market."</p>
              <div class="story-author">
                <div class="author-info">
                  <strong>Healthcare Innovator</strong>
                  <span>PocketGenes</span>
                </div>
              </div>
            </div>
          </div>

          <div class="story-card">
            <div class="story-content">
              <div class="quote-icon">"</div>
              <p>"Working with Golden Crow VS was seamless. They delivered our gym management app on time and it has significantly improved our member experience."</p>
              <div class="story-author">
                <div class="author-info">
                  <strong>Operations Director</strong>
                  <span>Global Fitness Chain</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Client Logos Carousel -->
    <section class="clients-section">
      <div class="container">
        <h2>Trusted By Industry Leaders</h2>
        <div class="logo-carousel">
          <div class="logo-track">
            <div class="client-logo">
              <span class="logo-text">StrongerU</span>
            </div>
            <div class="client-logo">
              <span class="logo-text">PocketGenes</span>
            </div>
            <div class="client-logo">
              <span class="logo-text">Anytime Fitness</span>
            </div>
            <div class="client-logo">
              <span class="logo-text">Golden Crow</span>
            </div>
            <div class="client-logo">
              <span class="logo-text">StrongerU</span>
            </div>
            <div class="client-logo">
              <span class="logo-text">PocketGenes</span>
            </div>
            <div class="client-logo">
              <span class="logo-text">Anytime Fitness</span>
            </div>
            <div class="client-logo">
              <span class="logo-text">Golden Crow</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- FAQ Section -->
    <section class="faq-section">
      <div class="container">
        <h2>What To Expect Next</h2>

        <div class="faq-list">
          <div class="faq-item">
            <button class="faq-question">
              <span>How does the consultation work?</span>
              <svg class="faq-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M19 9l-7 7-7-7"/>
              </svg>
            </button>
            <div class="faq-answer">
              <p>During the 30-minute video call, we'll discuss your app idea in detail, understand your business goals, identify key features, and provide initial recommendations. We'll also explain our development process and answer any questions you have.</p>
            </div>
          </div>

          <div class="faq-item">
            <button class="faq-question">
              <span>What should I prepare for the call?</span>
              <svg class="faq-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M19 9l-7 7-7-7"/>
              </svg>
            </button>
            <div class="faq-answer">
              <p>Think about your app's main purpose, target users, and key features. If you have any reference apps or designs, have them ready to share. Don't worry if you don't have everything figured out - that's what we're here to help with!</p>
            </div>
          </div>

          <div class="faq-item">
            <button class="faq-question">
              <span>What happens after the consultation?</span>
              <svg class="faq-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M19 9l-7 7-7-7"/>
              </svg>
            </button>
            <div class="faq-answer">
              <p>After our call, we'll send you a detailed proposal including scope, timeline, and investment. If you decide to move forward, we'll start with a discovery phase to create detailed specifications before development begins.</p>
            </div>
          </div>

          <div class="faq-item">
            <button class="faq-question">
              <span>Is there any commitment?</span>
              <svg class="faq-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M19 9l-7 7-7-7"/>
              </svg>
            </button>
            <div class="faq-answer">
              <p>Absolutely no commitment required. The consultation is completely free and designed to help you understand your options. We believe in building relationships based on mutual value, not pressure.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section class="contact-section">
      <div class="container">
        <h2>Questions Before Your Call?</h2>
        <p>Feel free to reach out to us anytime</p>
        <div class="contact-methods">
          <a href="mailto:support@goldencrowvs.com" class="contact-method">
            <span class="contact-icon">✉️</span>
            <span>support@goldencrowvs.com</span>
          </a>
        </div>
      </div>
    </section>
  </main>

  <footer class="main-footer">
    <div class="container">
      <div class="footer-content">
        <div class="footer-brand">
          <div class="logo">
            <img src="/logo1024.png" alt="Golden Crow VS Logo" />
          </div>
          <p>Transforming ideas into powerful mobile experiences.</p>
        </div>
        <div class="footer-links">
          <div class="footer-section">
            <h4>Company</h4>
            <ul>
              <li><a href="/about">About Us</a></li>
              <li><a href="/contact">Contact</a></li>
            </ul>
          </div>
          <div class="footer-section">
            <h4>Products</h4>
            <ul>
              <li><a href="/pocket-genes">Pocket Genes</a></li>
            </ul>
          </div>
        </div>
      </div>
      <div class="footer-bottom">
        <p>&copy; 2024 Golden Crow VS. All rights reserved.</p>
        <p class="footer-tagline">Developed with excellence and passion</p>
      </div>
    </div>
  </footer>
</body>
</html>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Mobile menu toggle
    const mobileMenuToggle = document.querySelector('.mobile-menu-toggle');
    const navLinks = document.querySelector('.nav-links');

    if (mobileMenuToggle && navLinks) {
      mobileMenuToggle.addEventListener('click', function() {
        navLinks.classList.toggle('active');
        mobileMenuToggle.classList.toggle('active');
      });

      const navLinksElements = navLinks.querySelectorAll('.nav-link');
      navLinksElements.forEach(link => {
        link.addEventListener('click', function() {
          navLinks.classList.remove('active');
          mobileMenuToggle.classList.remove('active');
        });
      });

      document.addEventListener('click', function(event) {
        if (!mobileMenuToggle.contains(event.target) && !navLinks.contains(event.target)) {
          navLinks.classList.remove('active');
          mobileMenuToggle.classList.remove('active');
        }
      });
    }

    // FAQ accordion
    const faqItems = document.querySelectorAll('.faq-item');
    faqItems.forEach(item => {
      const question = item.querySelector('.faq-question');
      question.addEventListener('click', () => {
        const isOpen = item.classList.contains('active');
        faqItems.forEach(otherItem => otherItem.classList.remove('active'));
        if (!isOpen) {
          item.classList.add('active');
        }
      });
    });
  });
</script>

<style>
  :root {
    --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --secondary-gradient: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
    --accent-gradient: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
    --success-gradient: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);

    --dark-bg: #0a0e27;
    --dark-surface: #1a1f3a;
    --dark-elevated: #2d3561;
    --text-primary: #ffffff;
    --text-secondary: #b8c5d1;
    --text-accent: #64b5f6;

    --glass-bg: rgba(255, 255, 255, 0.1);
    --glass-border: rgba(255, 255, 255, 0.2);
    --glass-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    font-size: 16px;
    scroll-behavior: smooth;
    overflow-x: hidden;
  }

  body {
    background: var(--dark-bg);
    background-image:
      radial-gradient(circle at 25% 25%, #667eea20 0%, transparent 50%),
      radial-gradient(circle at 75% 75%, #764ba240 0%, transparent 50%);
    color: var(--text-primary);
    font-family: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    overflow-x: hidden;
    min-width: 320px;
  }

  /* Navigation */
  .main-nav {
    background: rgba(26, 31, 58, 0.9);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
  }

  .nav-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: clamp(0.75rem, 2vw, 1rem) clamp(1rem, 4vw, 2rem);
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-shrink: 0;
  }

  .logo a {
    display: flex;
    align-items: center;
  }

  .logo img {
    height: clamp(32px, 5vw, 40px);
    width: auto;
    max-width: clamp(80px, 15vw, 120px);
    filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.3));
    border-radius: 4px;
  }

  .mobile-menu-toggle {
    display: none;
    flex-direction: column;
    gap: 4px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 8px;
  }

  .hamburger-line {
    width: 24px;
    height: 2px;
    background: var(--text-primary);
    transition: all 0.3s ease;
    transform-origin: center;
  }

  .mobile-menu-toggle.active .hamburger-line:first-child {
    transform: rotate(45deg) translate(6px, 6px);
  }

  .mobile-menu-toggle.active .hamburger-line:nth-child(2) {
    opacity: 0;
  }

  .mobile-menu-toggle.active .hamburger-line:last-child {
    transform: rotate(-45deg) translate(6px, -6px);
  }

  .nav-links {
    display: flex;
    gap: clamp(0.5rem, 3vw, 2rem);
    flex-wrap: wrap;
  }

  .nav-link {
    color: var(--text-secondary);
    text-decoration: none;
    font-weight: 500;
    transition: all 0.3s ease;
    position: relative;
    font-size: clamp(0.8rem, 2vw, 1rem);
    white-space: nowrap;
  }

  .nav-link:hover {
    color: var(--text-primary);
  }

  /* Container */
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 clamp(1rem, 4vw, 2rem);
  }

  /* Success Hero Section */
  .success-hero {
    position: relative;
    min-height: clamp(400px, 60vh, 600px);
    display: flex;
    align-items: center;
    overflow: hidden;
    margin-top: clamp(60px, 10vw, 80px);
    padding: clamp(3rem, 6vw, 5rem) 0;
  }

  .hero-bg-effects {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 1;
  }

  .floating-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(60px);
    opacity: 0.4;
    animation: floating 20s ease-in-out infinite;
  }

  .orb-1 {
    width: 300px;
    height: 300px;
    background: linear-gradient(135deg, rgba(67, 233, 123, 0.3) 0%, rgba(56, 249, 215, 0.2) 100%);
    top: 20%;
    left: 5%;
  }

  .orb-2 {
    width: 220px;
    height: 220px;
    background: linear-gradient(135deg, rgba(79, 172, 254, 0.25) 0%, rgba(67, 233, 123, 0.15) 100%);
    bottom: 20%;
    right: 10%;
    animation-delay: -8s;
  }

  .orb-3 {
    width: 160px;
    height: 160px;
    background: linear-gradient(135deg, rgba(67, 233, 123, 0.3) 0%, rgba(79, 172, 254, 0.18) 100%);
    top: 60%;
    left: 70%;
    animation-delay: -15s;
  }

  @keyframes floating {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    25% { transform: translateY(-40px) rotate(90deg); }
    50% { transform: translateY(25px) rotate(180deg); }
    75% { transform: translateY(-20px) rotate(270deg); }
  }

  .success-content {
    position: relative;
    z-index: 2;
    text-align: center;
  }

  .success-icon {
    width: 100px;
    height: 100px;
    margin: 0 auto 2rem;
    background: var(--success-gradient);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    animation: pulse 2s ease-in-out infinite;
  }

  .success-icon svg {
    width: 50px;
    height: 50px;
    color: white;
  }

  @keyframes pulse {
    0%, 100% { transform: scale(1); box-shadow: 0 0 0 0 rgba(67, 233, 123, 0.4); }
    50% { transform: scale(1.05); box-shadow: 0 0 30px 10px rgba(67, 233, 123, 0.2); }
  }

  .success-content h1 {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 800;
    margin-bottom: 1.5rem;
    background: var(--success-gradient);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .success-message {
    font-size: clamp(1.1rem, 2.5vw, 1.4rem);
    color: var(--text-secondary);
    max-width: 700px;
    margin: 0 auto;
    line-height: 1.6;
  }

  /* Eligibility Section */
  .eligibility-section {
    padding: clamp(3rem, 8vw, 6rem) 0;
    background: linear-gradient(135deg, var(--dark-surface) 0%, var(--dark-elevated) 100%);
  }

  .eligibility-section h2 {
    text-align: center;
    font-size: clamp(1.75rem, 5vw, 3rem);
    font-weight: 700;
    margin-bottom: clamp(2rem, 4vw, 3rem);
    background: var(--primary-gradient);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .eligibility-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: clamp(1.5rem, 3vw, 2rem);
  }

  .eligibility-card {
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: 1rem;
    padding: clamp(1.5rem, 3vw, 2rem);
    text-align: center;
    transition: all 0.3s ease;
  }

  .eligibility-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--glass-shadow);
  }

  .eligibility-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }

  .eligibility-card h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
    color: var(--text-primary);
  }

  .eligibility-card p {
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* Success Stories Section */
  .stories-section {
    padding: clamp(3rem, 8vw, 6rem) 0;
  }

  .stories-section h2 {
    text-align: center;
    font-size: clamp(1.75rem, 5vw, 3rem);
    font-weight: 700;
    margin-bottom: 1rem;
    background: var(--secondary-gradient);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .section-subtitle {
    text-align: center;
    font-size: clamp(1rem, 2.5vw, 1.25rem);
    color: var(--text-secondary);
    margin-bottom: clamp(2rem, 4vw, 3rem);
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
  }

  .stories-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: clamp(1.5rem, 3vw, 2rem);
  }

  .story-card {
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: 1rem;
    padding: clamp(1.5rem, 3vw, 2rem);
    transition: all 0.3s ease;
  }

  .story-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--glass-shadow);
  }

  .story-content {
    position: relative;
  }

  .quote-icon {
    font-size: 4rem;
    font-family: Georgia, serif;
    background: var(--accent-gradient);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    line-height: 1;
    margin-bottom: -1rem;
  }

  .story-content p {
    color: var(--text-secondary);
    line-height: 1.6;
    font-style: italic;
    margin-bottom: 1.5rem;
  }

  .story-author {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .author-info {
    display: flex;
    flex-direction: column;
  }

  .author-info strong {
    color: var(--text-primary);
    font-weight: 600;
  }

  .author-info span {
    color: var(--text-accent);
    font-size: 0.9rem;
  }

  /* Client Logos Carousel */
  .clients-section {
    padding: clamp(3rem, 8vw, 6rem) 0;
    background: linear-gradient(135deg, var(--dark-surface) 0%, var(--dark-elevated) 100%);
    overflow: hidden;
  }

  .clients-section h2 {
    text-align: center;
    font-size: clamp(1.75rem, 5vw, 3rem);
    font-weight: 700;
    margin-bottom: clamp(2rem, 4vw, 3rem);
    background: var(--accent-gradient);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .logo-carousel {
    overflow: hidden;
    position: relative;
  }

  .logo-carousel::before,
  .logo-carousel::after {
    content: '';
    position: absolute;
    top: 0;
    bottom: 0;
    width: 100px;
    z-index: 2;
  }

  .logo-carousel::before {
    left: 0;
    background: linear-gradient(to right, var(--dark-surface), transparent);
  }

  .logo-carousel::after {
    right: 0;
    background: linear-gradient(to left, var(--dark-surface), transparent);
  }

  .logo-track {
    display: flex;
    animation: scroll 20s linear infinite;
  }

  @keyframes scroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  .client-logo {
    flex-shrink: 0;
    padding: 1rem 3rem;
  }

  .logo-text {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text-secondary);
    white-space: nowrap;
    opacity: 0.6;
    transition: opacity 0.3s ease;
  }

  .client-logo:hover .logo-text {
    opacity: 1;
  }

  /* FAQ Section */
  .faq-section {
    padding: clamp(3rem, 8vw, 6rem) 0;
  }

  .faq-section h2 {
    text-align: center;
    font-size: clamp(1.75rem, 5vw, 3rem);
    font-weight: 700;
    margin-bottom: clamp(2rem, 4vw, 3rem);
    background: var(--primary-gradient);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .faq-list {
    max-width: 800px;
    margin: 0 auto;
  }

  .faq-item {
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: 1rem;
    margin-bottom: 1rem;
    overflow: hidden;
    transition: all 0.3s ease;
  }

  .faq-question {
    width: 100%;
    padding: 1.5rem;
    background: none;
    border: none;
    color: var(--text-primary);
    font-size: 1.1rem;
    font-weight: 600;
    text-align: left;
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-family: inherit;
  }

  .faq-icon {
    width: 24px;
    height: 24px;
    transition: transform 0.3s ease;
  }

  .faq-item.active .faq-icon {
    transform: rotate(180deg);
  }

  .faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease, padding 0.3s ease;
  }

  .faq-item.active .faq-answer {
    max-height: 500px;
    padding: 0 1.5rem 1.5rem;
  }

  .faq-answer p {
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* Contact Section */
  .contact-section {
    padding: clamp(3rem, 8vw, 6rem) 0;
    background: linear-gradient(135deg, var(--dark-surface) 0%, var(--dark-elevated) 100%);
    text-align: center;
  }

  .contact-section h2 {
    font-size: clamp(1.75rem, 5vw, 3rem);
    font-weight: 700;
    margin-bottom: 1rem;
    background: var(--success-gradient);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .contact-section > .container > p {
    font-size: clamp(1rem, 2.5vw, 1.25rem);
    color: var(--text-secondary);
    margin-bottom: 2rem;
  }

  .contact-methods {
    display: flex;
    justify-content: center;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .contact-method {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 2rem;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: 0.75rem;
    text-decoration: none;
    color: var(--text-primary);
    transition: all 0.3s ease;
  }

  .contact-method:hover {
    transform: translateY(-3px);
    box-shadow: var(--glass-shadow);
  }

  .contact-icon {
    font-size: 1.5rem;
  }

  /* Footer */
  .main-footer {
    background: var(--dark-surface);
    padding: clamp(2rem, 4vw, 3rem) 0 clamp(1rem, 2vw, 1rem);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  .footer-content {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: clamp(1.5rem, 3vw, 2rem);
    margin-bottom: clamp(1.5rem, 3vw, 2rem);
  }

  .footer-brand p {
    color: var(--text-secondary);
    margin-top: 0.5rem;
  }

  .footer-links {
    display: flex;
    gap: clamp(1.5rem, 4vw, 3rem);
    flex-wrap: wrap;
  }

  .footer-section h4 {
    font-weight: 600;
    margin-bottom: 1rem;
    color: var(--text-primary);
  }

  .footer-section ul {
    list-style: none;
  }

  .footer-section ul li {
    margin-bottom: 0.5rem;
  }

  .footer-section ul li a {
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.3s ease;
  }

  .footer-section ul li a:hover {
    color: var(--text-primary);
  }

  .footer-bottom {
    padding-top: 2rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    text-align: center;
  }

  .footer-bottom p {
    color: var(--text-secondary);
  }

  .footer-tagline {
    margin-top: 0.5rem;
    font-style: italic;
    opacity: 0.8;
  }

  /* Responsive Design */
  @media (max-width: 900px) {
    .mobile-menu-toggle {
      display: flex;
    }

    .nav-links {
      position: absolute;
      top: 100%;
      left: 0;
      right: 0;
      background: rgba(26, 31, 58, 0.95);
      backdrop-filter: blur(20px);
      flex-direction: column;
      padding: 1rem;
      box-shadow: var(--glass-shadow);
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      transform: translateY(-100%);
      opacity: 0;
      visibility: hidden;
      transition: all 0.3s ease;
      z-index: 999;
    }

    .nav-links.active {
      transform: translateY(0);
      opacity: 1;
      visibility: visible;
    }

    .nav-link {
      font-size: 1rem;
      padding: 0.75rem 1rem;
      border-radius: 0.5rem;
    }

    .nav-link:hover {
      background: rgba(255, 255, 255, 0.1);
    }
  }

  @media (max-width: 768px) {
    .footer-content {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .footer-links {
      justify-content: center;
    }
  }

  @media (max-width: 480px) {
    .orb-1, .orb-2, .orb-3 {
      display: none;
    }

    .eligibility-grid,
    .stories-grid {
      grid-template-columns: 1fr;
    }

    .success-icon {
      width: 80px;
      height: 80px;
    }

    .success-icon svg {
      width: 40px;
      height: 40px;
    }
  }

  @media (min-width: 1400px) {
    .container {
      max-width: 1400px;
    }

    .nav-container {
      max-width: 1400px;
    }
  }
</style>
