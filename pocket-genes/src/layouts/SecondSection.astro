<section id="screen2" class="interactive">
  <div class="second-bg-effects">
    <div class="floating-orb orb-1"></div>
    <div class="floating-orb orb-2"></div>
    <div class="floating-orb orb-3"></div>
    <div class="dna-helix"></div>
  </div>
  <div class="content-wrapper">
    <div class="text">
      <h1 class="section-title">
        <span class="title-accent">Mobile & Interactive</span>
        <span class="title-main">Genomic Panels</span>
      </h1>
      <p class="section-description">
        Clear reports on health, nutrition, ancestry, and more—making genomics
        easy to explore and understand for your users.
      </p>
      <div class="feature-highlights">
        <div class="highlight-item">
          <span class="highlight-icon">📊</span>
          <span>Interactive Reports</span>
        </div>
        <div class="highlight-item">
          <span class="highlight-icon">🎨</span>
          <span>Beautiful UI</span>
        </div>
      </div>
    </div>
    <div class="image-wrapper">
      <div class="image-card">
        <div class="lightning"></div>
        <img src="/interactive1.png" alt="Pocket Genes App" />
      </div>
    </div>
  </div>
</section>

<style>
  .interactive {
    position: relative;
    min-height: 100vh;
    background: linear-gradient(135deg, var(--dark-surface) 0%, var(--dark-elevated) 100%);
    background-image: 
      radial-gradient(circle at 20% 30%, rgba(103, 126, 234, 0.15) 0%, transparent 60%),
      radial-gradient(circle at 80% 70%, rgba(79, 172, 254, 0.12) 0%, transparent 60%),
      radial-gradient(circle at 50% 50%, rgba(240, 147, 251, 0.08) 0%, transparent 70%);
    overflow: hidden;
    display: flex;
    align-items: center;
    padding: 5rem 8%;
  }

  /* Background Effects */
  .second-bg-effects {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 1;
  }

  .floating-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(60px);
    opacity: 0.4;
    animation: floating 30s ease-in-out infinite;
  }

  .orb-1 {
    width: 320px;
    height: 320px;
    background: linear-gradient(135deg, rgba(103, 126, 234, 0.25) 0%, rgba(79, 172, 254, 0.2) 100%);
    top: 15%;
    left: 5%;
    animation-delay: 0s;
  }

  .orb-2 {
    width: 240px;
    height: 240px;
    background: linear-gradient(135deg, rgba(240, 147, 251, 0.2) 0%, rgba(103, 126, 234, 0.15) 100%);
    bottom: 20%;
    right: 8%;
    animation-delay: -10s;
  }

  .orb-3 {
    width: 180px;
    height: 180px;
    background: linear-gradient(135deg, rgba(79, 172, 254, 0.3) 0%, rgba(240, 147, 251, 0.18) 100%);
    top: 60%;
    left: 70%;
    animation-delay: -20s;
  }

  .dna-helix {
    position: absolute;
    top: 10%;
    right: 10%;
    width: 120px;
    height: 300px;
    opacity: 0.06;
    background: repeating-linear-gradient(
      45deg,
      transparent,
      transparent 12px,
      rgba(255, 255, 255, 0.3) 12px,
      rgba(255, 255, 255, 0.3) 24px
    );
    animation: dnaFloat 18s linear infinite;
  }

  @keyframes floating {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    25% { transform: translateY(-50px) rotate(90deg); }
    50% { transform: translateY(30px) rotate(180deg); }
    75% { transform: translateY(-25px) rotate(270deg); }
  }

  @keyframes dnaFloat {
    from { transform: rotate(0deg) translateY(0); }
    to { transform: rotate(360deg) translateY(-20px); }
  }

  .content-wrapper {
    position: relative;
    z-index: 2;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 4rem;
  }

  /* TEXT STYLES */
  .text {
    flex: 1;
    max-width: 55%;
    position: relative;
  }

  .section-title {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 800;
    line-height: 1.2;
    margin: 0 0 2rem 0;
  }

  .title-accent {
    display: block;
    background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    font-size: 0.7em;
    font-weight: 600;
    letter-spacing: 1px;
    text-transform: uppercase;
    margin-bottom: 0.5rem;
  }

  .title-main {
    display: block;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .section-description {
    font-size: clamp(1.1rem, 2.5vw, 1.6rem);
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0 0 2.5rem 0;
    max-width: 90%;
  }

  .feature-highlights {
    display: flex;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .highlight-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: 1.5rem;
    padding: 1rem 1.5rem;
    box-shadow: var(--glass-shadow);
    transition: all 0.3s ease;
    color: var(--text-primary);
    font-weight: 500;
  }

  .highlight-item:hover {
    transform: translateY(-3px);
    box-shadow: 
      0 15px 35px -5px rgba(31, 38, 135, 0.4),
      0 0 0 1px rgba(255, 255, 255, 0.25);
  }

  .highlight-icon {
    font-size: 1.25rem;
  }

  /* IMAGE STYLES */
  .image-wrapper {
    flex: 1;
    max-width: 45%;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .image-card {
    position: relative;
    width: 100%;
    max-width: 400px;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: 2rem;
    box-shadow: var(--glass-shadow);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    overflow: hidden;
    transform: rotate(5deg);
    padding: 1rem;
  }

  .image-card:hover {
    transform: rotate(5deg) translateY(-10px) scale(1.05);
    box-shadow: 
      0 25px 50px -12px rgba(31, 38, 135, 0.4),
      0 0 0 1px rgba(255, 255, 255, 0.2);
    background: rgba(255, 255, 255, 0.15);
  }

  .image-card img {
    width: 100%;
    height: auto;
    border-radius: 1.5rem;
    transition: all 0.3s ease;
  }

  .image-card:hover img {
    transform: scale(1.02);
  }

  /* LIGHTNING SHIMMER EFFECT */
  .lightning {
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      to right,
      rgba(255, 255, 255, 0) 0%,
      rgba(255, 255, 255, 0.3) 50%,
      rgba(255, 255, 255, 0) 100%
    );
    transform: skewX(-20deg);
    transition: left 0.3s ease-out;
    z-index: 1;
  }

  .image-card:hover .lightning {
    left: 120%;
  }

  /* Responsive Design */
  @media (max-width: 1200px) {
    .content-wrapper {
      flex-direction: column;
      text-align: center;
      gap: 3rem;
    }

    .text {
      max-width: 100%;
    }

    .image-wrapper {
      max-width: 60%;
    }

    .feature-highlights {
      justify-content: center;
    }
  }

  @media (max-width: 768px) {
    .interactive {
      padding: 3rem 5%;
      min-height: 80vh;
    }

    .content-wrapper {
      gap: 2rem;
    }

    .image-wrapper {
      max-width: 80%;
    }

    .image-card {
      transform: rotate(2deg);
      max-width: 320px;
    }

    .image-card:hover {
      transform: rotate(2deg) translateY(-5px) scale(1.02);
    }

    .feature-highlights {
      gap: 1rem;
    }

    .highlight-item {
      padding: 0.75rem 1.25rem;
      font-size: 0.9rem;
    }

    .orb-1, .orb-2, .orb-3 {
      opacity: 0.25;
    }

    .dna-helix {
      display: none;
    }
  }

  @media (max-width: 480px) {
    .interactive {
      padding: 2rem 4%;
    }

    .feature-highlights {
      flex-direction: column;
      align-items: center;
      gap: 0.75rem;
    }

    .highlight-item {
      width: fit-content;
    }

    .image-card {
      max-width: 280px;
    }

    .orb-1, .orb-2, .orb-3 {
      opacity: 0.15;
    }
  }
</style>