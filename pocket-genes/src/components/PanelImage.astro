<div class="container">
  <div class="first_column">
    <div class="panel first">
      <p>Health & Wellness</p>
    </div>
    <div class="panel second">
      <p>Ancestry Traits</p>
    </div>
  </div>
  <div class="second_column">
    <div class="panel third">
      <p>Nutrition & Fitness</p>
    </div>
    <div class="central_panel">
      <p>In-App Reports</p>
    </div>
    <div class="panel fourth">
      <p>Community & Sharing</p>
    </div>
  </div>
  <div class="third_column">
    <div class="panel fifth">
      <p>Medication Response</p>
    </div>
    <div class="panel sixth">
      <p>Education & Learning</p>
    </div>
  </div>
</div>

<style>
  .container {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    min-height: clamp(400px, 40vh, 600px);
    width: 100%;
    max-width: 800px;
    margin: 0 auto;
    overflow: visible;
    position: relative;
  }

  .central_panel {
    z-index: 10;
    border: 0;
    border-radius: 100%;
    background-color: #1a36b4;
    padding: clamp(2rem, 4vw, 4rem);
    margin: clamp(0.5rem, 1vw, 1rem);
    width: clamp(180px, 18vw, 280px);
    height: clamp(180px, 18vw, 280px);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .central_panel p {
    text-align: center;
    font-size: clamp(1.2rem, 2.2vw, 2.5rem);
    margin: 0;
    line-height: 1.2;
    font-weight: 600;
  }

  .first_column {
    display: flex;
    justify-content: center;
    flex-direction: column;
    gap: clamp(1rem, 2vw, 2rem);
  }
  
  .second_column {
    display: flex;
    justify-content: space-between;
    flex-direction: column;
    align-items: center;
    gap: clamp(1rem, 2vw, 2rem);
  }

  .third_column {
    display: flex;
    justify-content: center;
    flex-direction: column;
    gap: clamp(1rem, 2vw, 2rem);
  }

  .panel {
    width: clamp(100px, 12vw, 120px);
    height: clamp(100px, 12vw, 120px);
    z-index: 10;
    outline-offset: clamp(0.8rem, 1.5vw, 1.5rem);
    border-radius: 100%;
    margin: clamp(1rem, 3vw, 3rem) clamp(1rem, 2vw, 2rem);
    padding: clamp(0.8rem, 1.5vw, 1.5rem);
    display: flex;
    align-items: center;
    justify-content: center;
    outline-width: clamp(2px, 0.4vw, 4px);
    outline-style: dotted;
  }

  .panel p {
    text-align: center;
    font-size: clamp(0.8rem, 1.2vw, 1.2rem);
    margin: 0;
    line-height: 1.2;
    font-weight: 500;
  }

  .first {
    outline-color: #6d9eeb;
    background-color: #6d9eeb;
  }

  .second {
    outline-color: #c27ba0;
    background-color: #c27ba0;
  }

  .third {
    outline-color: #e06666;
    background-color: #e06666;
  }

  .fourth {
    outline-color: #8e7cc3;
    background-color: #8e7cc3;
  }

  .fifth {
    outline-color: #93c47d;
    background-color: #93c47d;
  }

  .sixth {
    outline-color: #f6b26b;
    background-color: #f6b26b;
  }

  /* Extra small devices - smaller than iPhone SE */
  @media (max-width: 375px) {
    .container {
      flex-direction: column;
      min-height: auto;
      max-width: 100%;
      padding: 0.5rem;
      gap: 0.5rem;
    }
    
    .first_column, .second_column, .third_column {
      flex-direction: row;
      justify-content: center;
      align-items: center;
      gap: 0.2rem;
      flex-wrap: wrap;
    }
    
    .panel {
      width: 60px;
      height: 60px;
      margin: 0.2rem;
      padding: 0.3rem;
      outline-offset: 0.2rem;
      outline-width: 1px;
    }
    
    .panel p {
      font-size: 0.5rem;
      line-height: 1;
    }
    
    .central_panel {
      width: 100px;
      height: 100px;
      margin: 0.5rem;
      padding: 0.8rem;
      order: -1;
    }
    
    .central_panel p {
      font-size: 0.9rem;
      line-height: 1;
    }
  }

  /* Mobile devices - iPhone SE and similar small screens */
  @media (max-width: 480px) and (min-width: 376px) {
    .container {
      flex-direction: column;
      min-height: auto;
      max-width: 100%;
      padding: 1rem;
      gap: 1rem;
    }
    
    .first_column, .second_column, .third_column {
      flex-direction: row;
      justify-content: center;
      align-items: center;
      gap: 0.5rem;
      flex-wrap: wrap;
    }
    
    .panel {
      width: clamp(70px, 18vw, 90px);
      height: clamp(70px, 18vw, 90px);
      margin: clamp(0.3rem, 1vw, 0.5rem);
      padding: clamp(0.5rem, 1vw, 0.8rem);
      outline-offset: clamp(0.3rem, 1vw, 0.5rem);
      outline-width: clamp(1px, 0.3vw, 2px);
    }
    
    .panel p {
      font-size: clamp(0.6rem, 2vw, 0.8rem);
      line-height: 1.1;
    }
    
    .central_panel {
      width: clamp(120px, 30vw, 160px);
      height: clamp(120px, 30vw, 160px);
      margin: clamp(0.5rem, 1vw, 1rem);
      padding: clamp(1rem, 3vw, 2rem);
      order: -1;
    }
    
    .central_panel p {
      font-size: clamp(1rem, 3vw, 1.4rem);
    }
  }

  /* Tablet and medium screens */
  @media (max-width: 768px) and (min-width: 481px) {
    .container {
      max-width: 100%;
      padding: 1rem;
    }
    
    .panel {
      width: clamp(80px, 10vw, 100px);
      height: clamp(80px, 10vw, 100px);
      margin: clamp(0.5rem, 2vw, 1.5rem);
      outline-offset: clamp(0.5rem, 1vw, 1rem);
      outline-width: clamp(1px, 0.2vw, 3px);
    }
    
    .panel p {
      font-size: clamp(0.7rem, 1.5vw, 1rem);
    }
    
    .central_panel {
      width: clamp(140px, 16vw, 200px);
      height: clamp(140px, 16vw, 200px);
      padding: clamp(1.5rem, 3vw, 3rem);
    }
    
    .central_panel p {
      font-size: clamp(1.1rem, 2vw, 1.8rem);
    }
  }

  /* Large tablets and small desktops */
  @media (max-width: 1024px) and (min-width: 769px) {
    .container {
      max-width: 700px;
    }
    
    .panel {
      width: clamp(90px, 8vw, 110px);
      height: clamp(90px, 8vw, 110px);
    }
    
    .central_panel {
      width: clamp(160px, 15vw, 240px);
      height: clamp(160px, 15vw, 240px);
    }
  }
</style>
