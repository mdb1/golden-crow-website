<div class="separator-container">
    <span class="dot"></span>
    <span class="shape_separator"></span>
</div>

<style>
    :root {
        --separator-color: var(--card-border);
        --separator-width: 3px;
        --dot-size: 16px;
        --spacing: 0px;
    }

    .separator-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        position: relative;
        margin: var(--spacing);
        bottom: -3.25rem;
    }

    .shape_separator {
        height: 55vh;
        width: var(--separator-width);
        background: linear-gradient(180deg, transparent 0%, var(--separator-color) 20%, var(--separator-color) 80%, transparent 100%);
        margin: var(--spacing) -var(--spacing);
        border-radius: 2px;
        box-shadow: none;
    }

    .dot {
        width: var(--dot-size);
        height: var(--dot-size);
        background: var(--brand-color);
        border-radius: 50%;
        position: absolute;
        top: 0px;
        box-shadow: none;
        border: 1px solid var(--card-border);
    }

    @media screen and (max-width: 1440px) {
        .separator-container {
            display: none;
        }
    }
</style>
